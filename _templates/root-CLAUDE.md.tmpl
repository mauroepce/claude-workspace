# {{WORKSPACE_NAME}} — Master Conventions

<!--
  This file was generated by /setup. It will be auto-loaded by Claude Code
  on every session in this workspace, so what you write here is always in
  the agent's context.
-->

## Purpose

{{WORKSPACE_PURPOSE}}

## Layout

- Subprojects live under `{{SUBPROJECT_DIR}}/`.
- Each subproject has its own `CLAUDE.md` (conventions) and `STATUS.md` (live state).
- Root `INDEX.md` tracks all subprojects.
- See `docs/ARCHITECTURE.md` for the full pattern explanation.

## Conventions

- Stable conventions go in `CLAUDE.md` files (root and per-subproject).
- Live state goes in `STATUS.md` per subproject.
- Update `STATUS.md` at the end of every productive session — staleness defeats the system.
- Every key decision should land in `STATUS.md` with the **why**, not just the choice.

{{CUSTOM_RULES_SECTION}}

## Commands

- `/new-project <name>` — create a new {{SUBPROJECT_NOUN}}.
- `/status` — summary of all {{SUBPROJECT_NOUN}}s and their open TODOs.
{{EXTRA_COMMANDS_LIST}}

## When resuming work

If you're picking up after a break:
1. Read root `INDEX.md` for the bird's-eye view.
2. Read `STATUS.md` of the {{SUBPROJECT_NOUN}} you're working on.
3. Then start.
